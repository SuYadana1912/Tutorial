import { Box } from "@mui/material";

export default function MUIBox() {
    return (
        <Box sx={{padding:2, backgroundColor: "grey.200"}}>
            This is a box with padding and background.
        </Box>
    );
}